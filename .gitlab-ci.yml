pages:
  tags:
    - docker
    - pages
  only:
    - master
  image: boileaum/debian-jupyter
  script:
    - scripts/make_slides.sh
    - scripts/make_archive.sh --zip
  artifacts:
    paths:
      - public
